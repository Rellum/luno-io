'use strict'

const expect = require('chai').expect
const LunoConnector = require('./../lib/luno-connector')
const sinon = require('sinon')

// describe('Constructor', function () {
//   it('should be a constructor', function () {
//     const actual = new LunoConnector()
//
//     expect(actual).to.be.an('object')
//   })
// })

// describe('getNextMessage', function () {
//   it('should have a getNextMessage method', function () {
//     const lunoConnector = new LunoConnector()

//     expect(lunoConnector.getNextMessage).to.be.an('function')
//   })
//   it('should do stuff', function () {
//     // const W3cWebsocket = require('websocket').w3cwebsocket
//     const W3cWebsocketStub = sinon.stub()
//     const lunoConnector = new LunoConnector(W3cWebsocketStub)

//     expect(W3cWebsocketStub.calledWithNew()).to.be.true

//     expect(lunoConnector.getNextMessage).to.be.an('function')
//   })
// })
